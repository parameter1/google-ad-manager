const cleanDocs = require('./clean-docs');
const cleanType = require('./clean-type');
const serviceUrlInfo = require('./service-url-info');

module.exports = {
  cleanDocs,
  cleanType,
  serviceUrlInfo,
};
