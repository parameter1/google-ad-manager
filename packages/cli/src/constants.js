const ROOT_API_PATH = 'apis/ads/publisher';

const constants = Object.freeze({
  DOCS_URI: 'https://developers.google.com/ad-manager/api/reference',
  ROOT_API_PATH,
  ROOT_URI: `https://ads.google.com/${ROOT_API_PATH}`,
});

module.exports = constants;
